<Window x:Class="AppTkani.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppTkani"
        mc:Ignorable="d"
        Icon="/Resources/logo.png"

        Title="ООО Ткани" Height="700" Width="1300" MinHeight="480" MaxHeight="1080" MinWidth="640" MaxWidth="1920">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0 15 0 0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="500"/>
            </Grid.ColumnDefinitions>
            <Button Content="Вход в другую учётную запись" Name="GoToLoginBtn" Click="GoToLoginBtn_Click" FontSize="30" HorizontalAlignment="Left" Margin="10 0 0 0" Padding="5" Visibility="Collapsed"/>
            <TextBlock Name="FullNameText" Text="{Binding FullName}" FontSize="30" Grid.Column="1" TextAlignment="Right" VerticalAlignment="Center" HorizontalAlignment="Stretch" Margin="0 0 10 0"/>
        </Grid>
        <Frame Grid.Row="1" Name="MainFrame" NavigationUIVisibility="Hidden"/>
    </Grid>
</Window>
